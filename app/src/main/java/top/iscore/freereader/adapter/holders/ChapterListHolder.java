package top.iscore.freereader.adapter.holders;

import android.content.Context;
import android.view.ViewGroup;

import top.iscore.freereader.fragment.adapters.CommonViewHolder;
import xcvf.top.readercore.bean.Chapter;

public class ChapterListHolder extends CommonViewHolder<Chapter> {
    public ChapterListHolder(Context context, ViewGroup root, int layoutRes) {
        super(context, root, layoutRes);
    }

    @Override
    public void bindData(Chapter chapter, int position) {

    }
}
